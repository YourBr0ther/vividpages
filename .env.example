# VividPages Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# Application Settings
# ============================================
NODE_ENV=development
DOMAIN=localhost

# ============================================
# Service Ports
# ============================================
API_PORT=4000
FRONTEND_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# ============================================
# PostgreSQL Database
# ============================================
POSTGRES_DB=vividpages
POSTGRES_USER=vividpages
POSTGRES_PASSWORD=your_strong_password_here_change_me

# ============================================
# Redis Cache & Queue
# ============================================
# No password for development
# For production, uncomment and set:
# REDIS_PASSWORD=your_redis_password_here

# ============================================
# MinIO Object Storage
# ============================================
MINIO_ACCESS_KEY=vividpages_access
MINIO_SECRET_KEY=your_minio_secret_key_change_me_min_32_chars
MINIO_BUCKET=vividpages

# ============================================
# JWT Authentication
# ============================================
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_change_me

# ============================================
# API Key Encryption
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be 64 hex characters (32 bytes)
ENCRYPTION_KEY=your_encryption_key_here_must_be_64_hex_characters_change_me_0123456789abcdef

# ============================================
# Google OAuth (Optional)
# ============================================
# Get from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:4000/api/auth/google/callback

# ============================================
# Ollama (Local LLM)
# ============================================
OLLAMA_HOST=http://ollama:11434

# ============================================
# Frontend
# ============================================
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000

# ============================================
# Worker Scaling
# ============================================
LLM_WORKERS=2
IMAGE_WORKERS=3

# ============================================
# Docker Build Target
# ============================================
BUILD_TARGET=development  # or 'production'

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. NEVER commit .env file to git (it's in .gitignore)
# 2. Use strong, unique passwords for all services
# 3. Generate random keys for JWT_SECRET and ENCRYPTION_KEY
# 4. Backup your ENCRYPTION_KEY securely - if lost, API keys are unrecoverable
# 5. For production, use much stronger passwords (32+ chars, random)
